import { IsbnFormatePipe } from './isbn-formate.pipe';

describe('IsbnFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new IsbnFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
