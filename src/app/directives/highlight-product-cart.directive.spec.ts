import { HighlightProductCartDirective } from './highlight-product-cart.directive';

describe('HighlightProductCartDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightProductCartDirective();
    expect(directive).toBeTruthy();
  });
});
